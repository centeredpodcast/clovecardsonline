$("h1").html("Hello!") //This is just filler stuff.

const cards = {
    "name": {
        "sprite": "imagelink",
        "otherData": "lol"
    }
}; //This will eventually get all of the cards possible and their data!!
let yourCardsData = ["name", "secondone", "lol", "secondone"]; //This will eventually be a list of all the cards you have.
let activeDeckData = 0; //Ths will eventually be the ID (if you will) of the deck that you are currently using.
let decksData = [["name", "secondone"], ["name"]]; //This will eventually be decks that you have.
